# ==============================================================================
# Environment Configuration Template
# Copy this file to '.env' and modify values as needed for your local environment.
# ==============================================================================

# Database Connection (PostgreSQL + pgvector)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ranking_db
DB_USER=postgres
DB_PASSWORD=password

# Spring Boot Backend Configuration
BACKEND_PORT=8080

# Flask Machine Learning Service Configuration
ML_HOST=localhost
ML_PORT=5000
